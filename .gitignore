node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz
.vercel

# Secrets live in .env locally and in Vercel project env vars
.env
.env.*
# ...except the template, which holds names only, never values
!.env.example

# Backup archives, too large for GitHub's 100MB file limit
*.zip

# Local agent tooling, not part of the project
.local
.agents

.vercel
.env*
